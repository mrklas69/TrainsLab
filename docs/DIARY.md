# DIARY — TrainsLab

Index sezení. Záznamy v `docs/diary/YYYY-MM-DD.md`.
Více sezení v jednom dni = sekce `## Sezení N` v témže souboru.

| Datum | Sezení | Obsah |
|-------|--------|-------|
| 2026-05-29 | 1 | Kick-off — koncept TrainsLab, mřížka experimentů, slack action jako nika, PoC plán (F0–F4), DD-01…05 |
| 2026-05-29 | 2 | Stavba PoC: F0 jednotělesová dynamika → F1 slack action → F2 trakce & adheze. DD-06, DD-07 |
| 2026-05-29 | 3 | Reality check: reverz = protiproudé brzdění, brzda = řízené tření, prototyp AudioView. DD-08, DD-09 |
| 2026-05-29 | 4 | Publikace dema na GitHub Pages: base `/TrainsLab/`, Actions deploy, oprava `build_type: legacy → workflow` |
| 2026-05-29 | 5 | README, slider sklonu (live rebuild tratě), vizualizace napětí ve spřáhlech, stavový semafor loko, skid při protiproudém brzdění. DD-10 |
| 2026-05-29 | 6 | `%AUDIT:CODE` (opravy D1–D3, K1–K3), diskuse omezení rychlosti (chybí otáčkový strop), návrh příčné dynamiky & vykolejení. DD-11 |
| 2026-05-29 | 7 | Příčná dynamika: `Track.radius(s)` (křivost horizontálního průmětu) + `lateralAcceleration` diagnostika. Žebřík opuštění monorailu (kývání = Úr. 1, drží DD-02) |
| 2026-05-29 | 8 | Esíčko + most: trať = ležatá osmička (Bernoulli), kritérium převrácení → fail state (vykolejení). DD-12 |
| 2026-05-30 | 9 | Kývání skříně (roll/pitch jako tlumené torzní oscilátory, `Track.signedCurvature`), gradient blízkosti meze (žár ∝ v²·κ), tuning vykolejení, minimalizace panelu. DD-13 |
| 2026-05-30 | 10 | Ovládání kamery (WASD/QE/ZX, held-key v Rendereru), F3 palivo (uhlí + voda, parní tlak). Souběžné sezení z téže báze — kývání duplikováno, ponecháno S9. DD-14 |
| 2026-05-30 | 11 | Otáčkový strop rychlosti — `v_mech = maxPistonSpeed·π·D/(2·zdvih)`, tah padá k mezní rychlosti (kolo = převod). Uzavřeno téma rychlosti z S6. DD-15 |
| 2026-05-30 | 12 | `%AUDIT:CODE` (0 kritických) — sjednocení příčné dynamiky na `signedCurvature` (radius odstraněn), `positionAt`, drobný úklid (R1/R2/K1–K3). K4 README číslování ponecháno (volba a) |
| 2026-05-31 | 13 | `%AUDIT:DOCS` (0 kritických) — D1 zastaralý `r≈33 m`, K1–K3 drobnosti; IDEAS/TODO pruning (P1–P4) — narovnání DRY TODO×IDEAS, nezralé nápady single-source v IDEAS. Údržbové, bez kódu |
| 2026-05-31 | 14 | Písek + mokrá kolej (`railFactor`, písek = spotřební zásoba, held-key P) — proměnná adheze pro tah i brzdu. Skid při brzdě (izomorfismus tah↔brzda). UX redesign (status/bar/modal, CSS grid). Auto-kamera „dron" → TODO. DD-16, DD-17, DD-18 |
| 2026-05-31 | 15 | Auto-kamera „dron" (toggle `C`) — sleduje soupravu zezadu-shora, lookAt střed, přelet při reverzu z tlumeného dohánění, hystereze směru u v≈0. Dron params mimo fyziku (DD-19), slider dvouzdrojový. První kus F4 |
| 2026-05-31 | 16 | `%CALIBRATE` (první za život projektu) — založeny `docs/PROMPTS.md` (%BEGIN/%END), `docs/DESIGN_DECISIONS.md` (rejstřík DD-01…19), projektový `CLAUDE.md` (izomorfismus s PocketStory); permission cleanup; paměti povýšeny do gitu. Údržbové, bez kódu |
| 2026-05-31 | 17 | F4 svět: lowpoly terén (heightfield, faceted), párové kolejnice + pražce, **trať vede po povrchu** (`Y=terrainHeight+most`, sklony z krajiny — DD-20), most u křížení s emergentními pilíři, stromy + kameny (`InstancedMesh` mimo trať). DD-20 |
| 2026-05-31 | 18 | `%AUDIT:CODE` (LOC práh, 0 funkčních bugů) — zastaralé komentáře/label po DD-20/DD-18 (K1 UI label „výška mostu", D2/D3) + mikro-cleanup (KO1 `grade` bez pozice, KO2 sdílená geometrie spřáhel); KO3 split `Renderer.ts` → backlog. Údržbové |
| 2026-05-31 | 19 | **Rázy z trati** (nápad uživatele) — dilatační spáry (klikot → pitch) + skok křivosti / výhybky (boční trh → roll) sjednoceny do jednoho balíku: impulsy do existujících kývacích oscilátorů (DD-21, rozšíření DD-13), jeden `crossed()` test, κ-skok fenomenologicky. Zvuk: tikot spár, skřípění oblouku, clunk. „Působí to věrně." |
| 2026-05-31 | 20 | **Doladění rázů z trati** — rozštěp perturbací na `PerturbationKind` (`transition`/`switch`); slider „kvalita přechodnic" (`transitionQuality`, tlumí jen κ-trh, default 0,3); zvuk rozdělen (`switchFired` clunk vs. `transitionJerkFired` skřípnutí `playArcJerk`); pozice ověřeny profilem κ (`u=0.25/0.75` = křížení, ne vrchol laloku — oprava komentářů S19). Dotažení DD-21 |
| 2026-05-31 | 21 | **Příprava sample vrstvy `AudioView`** (bez kódu) — manifest 8 zvuků 1:1 s hlasy (one-shot vs. loop), délky + charakter + licence do `IDEAS.md`; volba knihoven (Pixabay/Freesound CC0, vyhnout se BBC RemArc / NC / Zapsplat); fallback = hybrid (sample → padni na procedurální). Uživatel shání soubory |
| 2026-06-01 | 22 | **Modely vozů** (přechod na stroj mrkla — kontinuita ověřena) — lowpoly faceted factory `view/carModels.ts` (loko/cisterna/krytý/plošinový-plato/otevřený), souprava 6 těles, kola s valením + **hnací spojnice** loko + **animace prokluzu** (`driverSlipPhase`). Typ vozu = view metadata 1:1 s tělesy (DD-22). „Prokluzy parádní!" |
| 2026-06-01 | 23 | **Propracovaný kouř** — `SmokeView` (faceted obláčky, world-space → vlečka emergentně), hustota/tmavost ∝ výkon, idle pára. Sladěn se zvukem přes **`ExhaustClock`** = sdílený fyzikální rytmus výfuku (4×/otáčku kola), jediný zdroj pro kouř i chuff (DD-23, zpřesnil i zvuk). Bonus: dorazil **první sample** (`steam_chuff.wav`) → rozjeta **hybrid sample vrstva** (`loadSample`/`playSample`, fallback na procedurální). `chimneyTip` přes `getWorldPosition` (poučení S22) |
| 2026-06-01 | 24 | **Sample vrstva — 3 nové zvuky** (4 z 8): únik páry (`makeSampleLoop`, loop 1–11 s řízený parním tlakem), houkačka (`playHorn` one-shot + tlačítko/klávesa H), brzdy (`makeRandomizedLoop` — náhodné hranice smyčky proti švu, `playbackRate` ∝ rychlost = otáčení kol, jen za jízdy). Skřípení brzd: sample → 3-frekvenční synteticky → sample s fallbackem. „Super! Mám to!" |
| 2026-06-01 | 25 | **`%AUDIT:CODE`** (0 kritických) — split `Renderer.ts` → `CameraController` (D2, KO3 z S18) + `AudioView` → `proceduralAudio` (D3); D1 DRY `throttleFraction`, K1–K3 cleanup, K4 memoizace zamítnuta. Po živém testu: chuff/kouř jen `steamPressure>0` (bug), **chuff-kulomet vyřešen** (`CHUFF_FUSE_SPEED` cap na sdíleném clocku), idle kouř na palivu (`fireLit`), brzdový `playbackRate` cap (vrtačka → `BRAKE_FUSE_SPEED`). `comHeight` 1,2→0,9 (méně náchylné vykolejení). Brzdný model oponován a ponechán (Coulomb → lineární správně). „Kudos!" **Dodatek:** klapot spár ze samplu (`clattering_wheels.wav`, loop + `playbackRate ∝ rychlost`, `makeRateLoop`) → 5 z 8 hlasů manifestu. |
| 2026-06-01 | 26 | **`μ(v)` brzdy + Davisův `B·v`** (oba „Příště" S25) — `brakeFadeFactor = (1−fade)+fade/(1+k·|v|)` škáluje brzdnou sílu (litinový špalík, tření klesá s rychlostí, `f(0)=1` zachová Coulomb S25, asymptota `1−fade`) → konkávní zpomalení; skid-check proti faded síle. Davisova rovnice dokompletována o lineární člen `−davisB·v` v `beginStep` (dojezd). Slidery v Odporech/Trakci. Žádné nové DD (laditelné členy odporu). „Test OK". **`%AUDIT:DOCS`** (první od S13): D1 `DONE.md` doplněn o S16/S18/S21/S23/S24, D2 README „5 vagonů", D3 odstraněn hotový Split z TODO, K1 DD-21 přeřazen, K2/K3 README. |
| 2026-06-01 | 27 | **Kompletace zvuku + odstranění procedurální vrstvy** — uživatel dodal zbylé samply → zapojeno 5 hlasů (clank, clunk, arc_jerk, arc_squeal `makeSampleLevelLoop`, steam_slip) = **8 z 8 manifestu**. Hybrid (sample→fallback) byl od S23 most; set kompletní → **smazán `proceduralAudio.ts`** (166 ř.) + všechny fallback větve, audio čistě sample-based (trade-off: chybí-li sample → ticho). Interfacy `SustainVoice`/`LevelVoice` přesunuty do `AudioView`. **Censure:** překlep `breaks`/`brakes` v názvu souboru = brzdy se nikdy nenačetly (padalo na squeal = „divně"). Brzdy zprovozněny + randomizace hranic (2s perioda). Nárazníky na 1/3. „Zvuk brzd je autentický." |
| 2026-06-01 | 28 | **`%CALIBRATE`** (2. za život projektu, ~12 sez. od S16) — meta-audit řídících docs/procesu. A1 (kritické): `%CALIBRATE` neměl globální definici (jen v PocketStory) → **povýšen do globálního `~/.claude/PROMPTS.md`** (DRY, 3 projekty) + tabulka maker v globálním `CLAUDE.md`. A2 (kritické): cadence prahy nikde zapsané (`%BEGIN` je odhadoval z diářů) → **explicitní prahy + ledger** v `docs/PROMPTS.md` (CODE ≥6/+250 LOC, DOCS/pruning ≥12, CALIBRATE ≥15), opraven rozbitý ukazatel kroku 3. A4 docs zdravé, A5 `CONTEXT.md` nezaváděn (KISS). Bez kódu. |
| 2026-06-01 | 29 | **Křivkový odpor v obloucích** (stale „Příště" S26→S28) — `R = −sign(v)·curveResistance·|κ|·m·g`: specifický odpor úměrný křivosti (Röcklův charakter `c/r`, ověřeno rešerší), **rychlostně nezávislý**. **Geometrický** člen vedle gravitace v `Body.beginStep` (z `s`/trati), oddělený od rychlostních Davisových členů — komentář rozdělen na geometrické/rychlostní skupiny. Doplnil `R=A+B·v+C·v²` o zatáčkový člen. Drží DD-02 (`κ` skalár, podélná síla, žádný příčný DOF). Default `0,15` m (~2× valivý v laloku). Slider v Odporech. Žádné nové DD. „Test OK". |
| 2026-06-02 | 31 | **Ztlumení clunk** (`CLUNK_GAIN`) + **`%AUDIT:CODE`** (6., 0 kritických, „opravit vše") — K1 `CLUNK_GAIN` konstanta, K2 sjednocení cross-productu kolejnic, D2 doc drift clunk v GLOSSARY, **D1 split `Renderer.ts` → `WorldView.ts`** (statická scéna vs. aktéři, KO3 z S18, SLAP jako S25). 21 modulů, „Test OK". Bez nových DD |
| 2026-06-01 | 30 | **Rotační setrvačnost hmot** (rotating mass factor, poslední fyzikální položka backlogu) — `m_eff = m·(1+λ)`: rotující kola/ojnice přidají k translační setrvačnosti (rešerší ověřeno: rychlostně nezávislé, vlak ~6–8 %, loko ~10 %). Mění **jen převod síla→zrychlení** v `Body.integrate` (`accel=force/m_eff`) — síly (gravitace/odpory/adheze) drží skutečné `m` přes `massOf`. **Per-vůz** (izomorfní s `massOf`): getter `rotatingFactorOf`, loko `λ=0,15` (hnací kola+ojnice) / vůz `0,06`. 2 slidery v Hmotnostech. Drží DD-02 (skalár, žádný DOF). Žádné nové DD (laditelný setrvačný člen). „Test OK". |
| 2026-06-02 | 32 | **F4 dotažení + dron orbit + fix prokluzu** (view-only) — **mlha** na horizontu (`Fog`, bělavý opar, schová okraj desky) + **mostovka** (plná betonová deska pod kolejí na estakádě; sdílený `elevatedSamples` s pilíři, DRY). **Markery napětí spřáhel → toggle** checkbox „Zobrazení" v Nastavení (default skryté = čistá scéna; `buildCheckbox`). **Dron: chase → orbit** kolem **lokomotivy** (konst. úhlová rychlost, `lookAt` loko; odpadla hystereze směru + reverz-přelet; DD-19 drží — mění se chování, ne umístění params); zoom v dronu Z/X i kolečkem myši (`adjustOrbitRadius`, DRY). **Bug fix:** prokluz kol se točil obráceně (znaménko `driverSlipPhase` — slip teď ve směru valení). Bez nových DD |
