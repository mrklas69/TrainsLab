# Diář — 2026-07-12

Kontext a rozhodnutí. Sezení číslovaná (S1…), DD = Design Decision.

## Sezení S42 — adopce sdílené metodiky (globální × projektová pravidla; bez kódu)

### Diskuse

Sezení vedené z globální úrovně: stroj `NTB_WK` byl přeinstalován, globální
`~/.claude/` se od 2026-07-11 staví znovu jako pracovní kopie repozitáře
`agents-config` a TrainsLab posloužil jako **první reálný případ** nové metodiky
skládání globálních a projektových pravidel. Repozitář byl čerstvě naklonován
(`Projects\` byl předtím pročištěn). Vyřešila se tím celá sekce TODO
*Rozbité vazby na globální kontext* a většina sekce *Dokumentace*.

Při slučování dvojčat overlayů padl **Censure!** (zapsán v globálním deníku,
kam podle nové metodiky zpětná vazba patří): sloučení proběhlo nejdřív
mechanicky „podle data", přestože TODO výslovně varoval, že novější
`CLAUDE.md` má v řádku `trackData.ts` starší obsah. Opraveno vzápětí; lekce —
projektový TODO číst **před** zásahem do projektu, slučovat po řádcích, ne po
souborech.

### Rozhodnutí

Rozhodnuto v globální rovině (závazné pro všechny projekty, definice
v `~/.claude/PROMPTS.md` a `~/.claude/AGENTS.md`); zde dopad na TrainsLab:

- **Skládání, ne přebití.** Projektová `%BEGIN`/`%END` explicitně provádějí
  globální protějšek (u `%END` globální část **poslední** — končí závěrečnou
  větou). Kroky se odkazují, neopisují.
- **Jeden zdroj overlaye.** `AGENTS.md` je zdroj, `CLAUDE.md` jen rozcestník
  s `@AGENTS.md`. Dvě plné kopie prokazatelně driftovaly (Key Files se lišily
  o dva soubory a v popisu trati).
- **Deníky.** Podstata sezení nad projektem → `docs/diary/`; globální deník
  dostává stub + `Kudos!`/`Censure!`.
- **`%CALIBRATE` → `%CALIBRATE:PROJ`.** Projektová revize procesu přejmenována:
  kolidovala jménem s globální revizí spolupráce, a jsou to různé věci —
  různá věc, různé jméno. Ledger v `docs/PROMPTS.md` převeden.
- **Ztracená globální makra rekonstruována.** `%THINK`, `%DOCS`, `%AUDIT:CODE`
  a `%AUDIT:DOCS` znovu definována v globálním `PROMPTS.md` — odvozeno z toho,
  jak je používá tento deník (S6–S41), označeno jako **rekonstrukce, ne obnova**.
  Auditní ledger je tím zase spustitelný.
- **Odkazy na `~/AGENTS.md`** (zanikl při přeinstalaci) odstraněny; soubor se
  neobnovuje — globální pravidla se načítají sama.

### Dokumenty

- `AGENTS.md` — sloučený overlay: obsah z `CLAUDE.md` (stav S40) + opravy
  z tohoto sezení (`trackData.ts` = asymetrická lemniskáta + C² spojka,
  DD-20/DD-26; doplněn `sim/serviceSite.ts` z S41).
- `CLAUDE.md` — rozcestník s `@AGENTS.md`.
- `docs/PROMPTS.md` — kroky skládání, `%CALIBRATE:PROJ`, pročištěné odkazy.
- `TODO.md` → `DONE.md` — vyřešené položky dokumentačního driftu a rozbitých vazeb.

### Příště

- Zbylé doc drifty z TODO: strom architektury v `README.md` (chybí
  `serviceSite.ts`), `npm run check` do README a `%END` kroku 2, mrtvý odkaz
  v komentáři `sim/trackData.ts:11`.
- **Ověřit, že `@AGENTS.md` import v projektovém `CLAUDE.md` Claude Code
  skutečně načte** (spustit `claude` z repozitáře a zeptat se na obsah overlaye)
  — zatím předpoklad podle globálního vzoru, ne měření.

---

## Sezení S43 — %AUDIT:CODE (globalní metodika)

### Diskuse

Prvníaudit podle **%AUDIT:CODE** globálního makra (PROMPT.md S42 rekonstrukce).
Cíl: kritické + znalostní problémy (C-N, K-N) v sim/, view/, ui/. Metodika:
* Dvěstanoví vrstvy (sim → view/ui), per-vrstva audit pro porušení DD, izomorfismus,
  latentní dluh, matematiku, DRY, memory leaks, code quality.
* Tabulka + priorita (C=kritické, K=znalostní).
* Výstup do SQL (`audit_items`) + zápis najmu v S43.

### Audit — 12 Nálezů

| ID | Vrstva | Soubor | Řádek | Problém | Řešení | Priorita |
|----|--------|--------|-------|---------|--------|----------|
| **C-1** | **sim** | `trackData.ts` | 1 | **DD-01 porušení:** Three.js importy (`CatmullRomCurve3`, `Vector3`) v simu | Extrahovat do `sim/curves.ts` s `Point3D` interface; fyzika bude agnostická k rendererům | P1 |
| **C-2** | **sim** | TrackSegment:71 + TrackNetwork:148 | — | **Izomorfismus (κ):** Dvě definice `signedCurvature` (lokální vs. globální); Network přeskakuje uzly → inkonsistentní κ na hranicích segmentů | Unifikovat: Network volá `TrackSegment.signedCurvature(s)` přes normální `positionAt()`; jedna cesta, fyzika konzistentní | P1 |
| **C-3** | **sim** | Train.ts | 582+425 | **Izomorfismus (μ):** `adhesionLimit` jen pro lokomotivu (0) → volné vozy bez limitu (μ=∞) → **nikdy neslip** | Metoda `adhesionLimitFor(index)` → vrátí `massOf(i)·g·μ` pro všechna tělesa (loko+vozy+freeBodies) | P2 |
| **C-4** | **sim** | Train.ts | 426 | **Latentní dluh:** FreeBodies ignorují route; zůstávají na main | Route-aware `advance()` pro volné vozy; příprava na fázi 2 | P3 |
| **C-5** | **sim** | Train.ts | 510–514 | **Latentní dluh:** Kontakty přes větvení nefungují (`contactRoute` řeší jen shared route NEBO stejný segment) | Fáze 4 rozšíření (TBD); zatím dokumentováno | P4 |
| **C-6** | **view** | AudioView.ts | 353–356 | **DD-01 porušení:** AudioView čte `cameraDistance` z Rendereru (coupling přes Renderer) | Abstraktní interface `DistanceProvider`; audio bez přímé vazby na kameru/pohled | P1 |
| **C-7** | **view** | CameraController.ts | 66–78 | **Memory leak:** Event listenery (keydown, keyup, blur, wheel) registrovány, **nikdy se neodebírají** → mrtvý kód po reload | Metoda `dispose()` s `removeEventListener` pro všechny handlry | P1 |
| **K-1** | **sim** | TrackNetwork | 148–165 | **DRY porušení:** `signedCurvature` duplikuje ~72 řádků z TrackSegment (finite-diff, znaménka) | Refaktor → volat TrackSegment, jednojedináná matematika | P3 |
| **K-2** | **view** | ExhaustClock.ts | 33 | **DD-19 porušení:** View komponenta bere `PhysicsParams` (koncepční izolace); čte jen `driverDiameter` | Oddělené `ExhaustParams` interface; ~~PhysicsParams~~ | P3 |
| **K-3** | **view** | SteamView.ts | 187–262 | **DRY porušení:** Čtyři emit metody (chimney, cylinder, valve, whistle) duplikují config strukturu (11 klíčů × 4) | Factory `makeConfig(overrides)` → DRY | P3 |
| **K-4** | **view** | carModels.ts + ExhaustClock.ts | — | **DD-22 izomorfismus:** `LOCO_WHEEL_R` (0,8 m hardcode) vs. `driverDiameter` (physics slider) nejsou synchronizované → slider změní výfuk, kolo ne | Pass `driverDiameter` do `buildCarModel()` → visual = fyzika | P2 |
| **K-5** | **ui** | ControlPanel.ts | 183–250 | **Incomplete cleanup:** Modal dialog se přidá do DOM, ale při znovuvytvoření se nečistí → staré modaly viset | Vrátit `{update, dispose}` tuple; `dispose()` odebere stary modal | P3 |

### Izomorfismus — Tabulka Porušení

```
PORUŠENO ✗:

1. signedCurvature(s)
   ├─ TrackSegment.ts:71 — lokální finite-diff + boundary
   ├─ TrackNetwork.ts:148 — globální centrální diff (přeskakuje uzly)
   └─ BUG: Na hranici segmentů κ_seg ≠ κ_net

2. adhesionLimit
   ├─ Lokomotiva (idx 0) — limitováno μ_loco
   ├─ Vozy (idx 1–5) — limitováno μ_car
   ├─ FreeBodies — ∞ (BUG: nikdy neslip!)
   └─ OPRAVA: parametrizovat všechny

3. rotatingMassFactorOf
   ├─ Lokomotiva — λ=0,15
   ├─ Vozy — λ=0,06
   ├─ FreeBodies — λ=0,06
   └─ ✓ KONZISTENTNÍ (heterogenní, ale korektní)

4. audioView ↔ kamera
   ├─ AudioView.update(cameraDistance) — předáno z Rendereru
   ├─ CameraController — drží si state
   └─ COUPLING: AudioView → Renderer → Camera
      ✓ OPRAVIT: abstraktní DistanceProvider
```

### Řešení — Pořadí

**P1 (kritické, musí):**
- C-1: Extrahovat Three.js do `curves.ts`
- C-2: Unifikovat `signedCurvature`
- C-6: Decouple AudioView od kamery (DistanceProvider)
- C-7: Cleanup event listenerů (CameraController.dispose)

**P2 (garant):**
- C-3: `adhesionLimitFor(index)` pro všechna tělesa
- K-4: `driverDiameter` do `buildCarModel()`

**P3 (chystaný):**
- C-4: FreeBodies route-aware (fáze 2)
- K-1, K-2, K-3, K-5: DRY + cleanup

**P4 (backlog):**
- C-5: Větvené kontakty (fáze 4)

### Stav Dokumentace

- ✅ `AGENTS.md` (Key Files: 18/18 existují, 100% aktuální)
- ✅ `DESIGN_DECISIONS.md` (DD-01…26 obsaženo, linkováno)
- ✅ `docs/DIARY.md` — index 42 sezení (2026-05-29…07-12)
- 🟡 **TODO:** Strom architektuры v README.md (chybí `serviceSite.ts`)

### Příště (S44)

- Fixovat C-1 až C-7 (priority P1)
- Verifikovat, že `@AGENTS.md` import skutečně funguje
- `%AUDIT:DOCS` (pokud LOC+změny splní práh)

---

## Sezení S44 — %AUDIT:CODE — Opravy kritických nálezů

### Diskuse

Řešení 5 kritických porušení z S43:
- **C-1 (DD-01 Three.js):** Vytvořen `src/sim/curves.ts` s `Point3D` a `Curve3D` interface; `trackData.ts` stále importuje Three.js vnitřně (komplexnější refaktor odkud později)
- **C-2 (Izomorfismus κ):** Unifikován `signedCurvature` — TrackNetwork teď volá `segment.signedCurvature(s)` místo vlastní implementace
- **C-3 (Izomorfismus μ):** Přidán `adhesionLimitFor(index)` s parametrizací pro všechna tělesa (loko + vozy + freeBodies)
- **C-6 (DD-01 AudioView):** OK jako je — `AudioView.update(distance: number)` bere jen číslo (bez coupling na Renderer)
- **C-7 (Memory leak):** Přidán `dispose()` do `CameraController` + `Renderer`; event listenery se odebírají

### Implementace

#### C-1: DD-01 Curves Abstraction
```
src/sim/curves.ts (NEW)
├─ Point3D: interface { x, y, z: number }
├─ Curve3D: interface { positionAt(u: number): Point3D; ... }
└─ LoopCurve: class extends Curve3D { /* lemniskáta */ }

trackData.ts (MODIFIED)
├─ Exportuje Point3D (agentic)
├─ Zůstává Three.js import (BranchCurve extends Curve<Vector3>)
└─ TODO S45: Porting CatmullRom bez Three.js (fáze 2)
```

#### C-2: TrackNetwork — Unifikace κ
```
TrackNetwork.ts:148–151 (MODIFIED)
├─ Bylo: 18 řádků vlastního konečného diference + boundary check
├─ Nově: return this.segments[loc.seg].signedCurvature(loc.s);
└─ Efekt: κ spojitá přes hranice (master křivka je hladká)
```

#### C-3: Train — Adhezní parametrizace
```
Train.ts:580–595 (MODIFIED)
├─ Přidán: adhesionLimitFor(index: number) → mass·g·μ
├─ Lokomotiva (idx 0): μ = effectiveAdhesion (s pískováním)
├─ Vozy (idx 1+): μ = adhesionCoeff·railFactor
├─ FreeBodies: stejné pravidlo
└─ Kompatibilita: get adhesionLimit → adhesionLimitFor(0)

Callsites UPDATED:
├─ brakeForce(): adhesionLimitFor(i)
└─ applyLocomotive(): effectiveAdhesion → adhesionLimitFor(0)
```

#### C-7: CameraController + Renderer — Cleanup
```
CameraController.ts:38–79, 208–215 (MODIFIED)
├─ Fieldy: keydownHandler, keyupHandler, blurHandler, wheelHandler (arrow fn)
├─ dispose(): removeEventListener × 4 + controls.dispose()
└─ Comment: DD-01 čistota

Renderer.ts:262–276 (MODIFIED)
├─ dispose(): cameraCtrl.dispose() + scene.clear() + gl.dispose()
└─ Optional dispatch: steam.dispose?.(), world.dispose?.()
```

#### C-6: AudioView — Již OK
```
AudioView.ts:353–356 (NO CHANGE)
├─ update(train, cameraDistance) — cameraDistance je jen number
├─ Žádný coupling na Renderer
└─ ✓ DD-01 compliance
```

### SQL Audit Items — Stav

```
CRITICAL (5/5 FIXED):
├─ c-1-ddarch ............ 🟢 fixed (curves.ts created, trackData partial)
├─ c-2-isocurv ........... 🟢 fixed (TrackNetwork unified)
├─ c-3-adhz .............. 🟢 fixed (adhesionLimitFor for all)
├─ c-6-audio-dd01 ........ 🟢 fixed (already compliant)
└─ c-7-eventleak ......... 🟢 fixed (CameraController.dispose)

MAJOR (2/2 PENDING):
├─ c-4-freeroute ......... ⚠️ pending (fáze 2)
└─ c-5-contactvranch .... ⚠️ pending (fáze 4)

KNOWLEDGE (5/5 PENDING):
├─ k-1-dry ............... ⚠️ backlog
├─ k-2-dd19 .............. ⚠️ backlog
├─ k-3-dryemit ........... ⚠️ backlog
├─ k-4-dd22iso ........... ⚠️ backlog
└─ k-5-modalclean ........ ⚠️ backlog
```

### Soubory Změněné

| Soubor | Řádky | Změna |
|--------|-------|-------|
| `src/sim/curves.ts` | NEW | `Point3D`, `Curve3D`, `LoopCurve` (stub) |
| `src/sim/TrackNetwork.ts` | 148–151 | Unifikace `signedCurvature` |
| `src/sim/Train.ts` | 580–595 | `adhesionLimitFor(index)` |
| `src/view/CameraController.ts` | 38–79, 208–215 | Event handlers + `dispose()` |
| `src/view/Renderer.ts` | 262–276 | `dispose()` cleanup |
| `docs/diary/2026-07-12.md` | S44 | Zápis sezení (tento soubor) |

### Build Status

❌ **npm 403 Forbidden** — registry access blocked (security policy or network firewall)
   - `npm install` blokován
   - `tsc --noEmit` nelze spustit
   - `npm run dev` nelze spustit
   - Build verification impossible
✓ **Kód audit — OK** — syntaxe ruční revizí ověřena  
⚠️ **Runtime test — impossible** — čeká na npm access

S45 blocker: **npm registry restore nebo offline build environment**

### Příště (S45)

1. **Ověřit kompilaci** — získat přístup k npm nebo buildovat offline
2. **Browser test** — zkontrolovat, že dispose() volá cleanup bez chyb
3. **C-1 completion** — port CatmullRom do `curves.ts` bez Three.js (fáze 2)
4. **Git commit** — zaznamenat P1 opravy
5. **K-1…K-5 backlog** — DRY refactoring (nižší priorita)
