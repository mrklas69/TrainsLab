# 2026-09-09

## Sezení 1 (Codex) — 17:46–19:13

Projektové pořadí: **S45**; denní číslování odpovídá současnému globálnímu formátu.

**Cíl:** Obnovit repozitář TrainsLab z GitHubu, provést `%CALIBRATE:PROJ`, po A24 zprovoznit a ověřit aplikaci a následně opravit nalezené zranitelnosti závislostí.

**Hotovo:**

- Ověřen prázdný pracovní adresář včetně absence `.git`; veřejné GitHub API potvrdilo `mrklas69/TrainsLab` a výchozí větev `main`.
- Naklonován `https://github.com/mrklas69/TrainsLab.git` do `D:\~PROJECT\TrainsLab`; následný fetch a status potvrdily shodu `main` s `origin/main`, commit `1cbb3eb` z 2026-07-12. Před zápisem deníku byl pracovní strom čistý; `git fsck --full` bez nálezů.
- Globální pravidla aktuální (pull bez změn), poslední globální sezení uzavřené; od globální kalibrace 2026-08-21 proběhlo 30 dalších sezení, s dnešním 31.
- Načten projektový overlay, makra, README, TODO, IDEAS a poslední diář včetně opravy tvrzení S43/S44.
- Dokončena `%CALIBRATE:PROJ` nad S29–S44 s referencí k S28. Výsledek a čtyři konkrétní návrhy pravidel: [kalibrace S45](../CALIBRATION_2026-09-09.md). Číselné prahy ponechány; ledger kalibrace posunut na S45. Index S43/S44 dorovnán s doloženou korekcí, u C-3 v TODO zapsána výhrada k premise.
- Po schválení **A18 Ano** zapracovány všechny čtyři změny do `AGENTS.md` a `docs/PROMPTS.md`; ověřovací příkazy dorovnány v README, související úkol přesunut z TODO do DONE. Znění pravidel v kalibračním reportu nahrazeno odkazy na zdroje (DRY).
- A24: nainstalován oficiální Node.js 22.23.2 / npm 10.9.8 (ZIP ověřen SHA256), cesta přidána do user PATH. Umístění a lokální spouštěč eviduje `~/.claude/env/STELLA.md`.
- `npm ci`, `npm run check` a `npm run build` prošly na obnoveném kódu `1cbb3eb`; lockfile beze změny. Regrese pokrývají síť/spojku, převrácení, pískování, doplnění při zastavení versus průjezd, branch route/zámek, rázy a plnou soupravu.
- Ověřený dev server na `http://127.0.0.1:5175/` přes junction a oba Node přepínače zachování symlinků. Běžný Chrome otevřen pro živý test. Problém Vite s `~` popsán v README; zdrojový kód ani konfigurace Vite se kvůli němu neměnily.
- Headless Chrome: 45 draw calls / 50 517 trojúhelníků, screenshoty 1440×960 a 390×844 vizuálně zkontrolovány; funkční přepnutí tras klávesami, rozjezd (~1,8 m/s), brzda a reset. Načteno všech 10 zvuků; RMS za master gainem ~0,00655 → mute 0 → unmute ~0,00672; píšťala při mute nespustila zdroj. Bez JS výjimek a neúspěšných HTTP odpovědí. Jde o měření zvukového signálu, nikoli uživatelovo sluchové přijetí.
- Dočasné podklady: `%TEMP%/trainslab-smoke-20260909/` (`browser-smoke.mjs`, `result.json`, `desktop.png`, `mobile.png`). Build hlásí známý chunk 569,67 kB (gzip 148,36 kB); není to chyba buildu.
- **A48: Test OK, zdá se, že vše funguje.** Uživatel přijal živý test obrazu a zvuku. Lokální `start-dev.ps1 -NoBrowser` ověřen proti běžícímu serveru; `.ps1` má UTF-8 BOM. Pomocné servery 5173/5174/4173 ukončeny, funkční 5175 ponechán.

- Na následný pokyn uživatele opraveny zranitelné tranzitivní dependencies příkazem `npm audit fix --no-fund` bez force. `esbuild` 0.28.0 → 0.28.2, `fflate` 0.6.10 → 0.6.11, `nanoid` 3.3.12 → 3.3.18, `postcss` 8.5.15 → 8.5.28; přímé závislosti a zdrojový kód beze změny. Větší textový diff lockfile plyne také z přesunu esbuild a jeho platformních balíčků z větve tsx do kořene stromu.
- Ověření opravy: čisté `npm ci --no-fund`, samostatné `npm audit --json` (0 ve všech závažnostech), `npm run check` i `npm run build` prošly. Restartovaný dev server na 5175; opakovaný Chrome test vykreslení, tras, jízdy, brzdy a resetu bez výjimek či HTTP chyb. Načteno 10 zvuků; RMS ~0,00628 → mute 0 → unmute ~0,00638. Známé varování velikosti chunku zůstává. Předchozí lockfile zálohován mezi dočasné podklady jako `package-lock.before-audit-fix.json`.

- `%END`: ukončen poslední dev server (PID 3036); porty 5173/5174/5175/4173/9224 bez listeneru, žádný zbývající Vite proces. Oprava závislostí commitnuta jako `2761bb4`; dokumentace následuje samostatně. Kontroly nebyly znovu opakovány po čistě dokumentačních úpravách.

**Rozhodnutí:**

- A18: podmínky přijetí před netriviální změnou, opora auditních nálezů, shoda lokálních kontrol s CI a jednoznačný ledger. Důvod: odstranění příčin chybných závěrů a nedotažených oprav S35–S44; podrobnosti v kalibračním reportu. Číselné prahy se nemění.

**Zpětná vazba:** V tomto sezení nepadlo Kudos! ani Censure!; není nový vzorec k vyhodnocení.

**Otevřené:**

- CODE ledger konzervativně S41 (odstup 4); zaznamenaný S43 má sporné závěry. DOCS/pruning S40: pět sezení, pod prahem 12. Kalibrace S45 právě dokončena. Původní měření 5 přidaných / 113 odstraněných řádků bylo pouze za opravné commity po S44, nikoli za celý interval od S41; nelze jím samostatně dokazovat auditní LOC práh.
  Správný interval `f7f0485..HEAD` (závěr S41 → obnovený stav) má v `src/` 52 přidaných / 24 odstraněných řádků; práh 250 nedosažen.
- Z dohry S43/S44 zbývá posouzení premisy C-3, volání `Renderer.dispose()` a strom architektury README. Doplnění `npm run check` do vývojového postupu je hotové. Žádná položka není doložena jako opakovaná v pěti posledních sezeních.
- Příští navržený řez: ověřit životní cyklus rendereru při HMR a potřebu zapojení `dispose()`; nejprve konkrétní reprodukce, pak návrh opravy.

**Pozor:**

- Původní S43/S44 obsahují nepravdivá tvrzení o dokončení oprav; rozhodující korekce je v závěrečné sekci Dohra S43/S44 deníku 2026-07-12. Index byl v S45 opraven. Historický údaj o zeleném CI není dnešní test.
- Spuštění PowerShellu v sandboxu selhalo už při CreateProcessAsUserW (-1073283067); příkazy proběhly s explicitní eskalací.

<!-- nové záznamy vkládej NAD tento řádek -->
