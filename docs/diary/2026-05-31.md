# Diář — 2026-05-31

Kontext a rozhodnutí. Sezení číslovaná (S1…), DD = Design Decision.

## Sezení S13 — `%AUDIT:DOCS` + IDEAS/TODO pruning (údržbové, bez kódu)

`%BEGIN` začal `git fetch` (lekce z S10) — lokál synchronní s `origin/main`, žádná kolize.
Cadence: dva překročené prahy — `%AUDIT:DOCS` (≥ 10 sezení, samostatný DOCS audit zatím **nikdy**)
a IDEAS/TODO pruning (≥ 12 sezení). Oba vyřešeny v tomto sezení. Čistě dokumentace, kód netknut.

### `%AUDIT:DOCS` — nálezy a opravy

Pročteno všech 8 `*.md`, křížově ověřeno proti kódu (`params.ts`, `Train.ts`, `trackData.ts`,
`main.ts`, `Renderer.ts`, `ControlPanel.ts`). Dokumentace v dobré kondici — průběžný `%DOCS` drží
README v synchronu (klávesy, architektura, slidery, odkazy vše ✅). Žádný kritický nález.

- **D1 (doporučené)** — `GLOSSARY.md` zastaralý poloměr laloků `r≈26 m` → **`r≈33 m`** (dohnán
  S9 rozvolnění `A,B` 120→150; kód i komentář v `trackData.ts` měly 33 už od S9).
- **K2 (kosmetické)** — `GLOSSARY.md` poměr spotřeby voda:uhlí `~4–6:1` → `~6:1` (sedí na
  default `waterRate 38 : coalRate 6`).
- **K3 (kosmetické)** — `DONE.md` přidána poznámka „S1 = koncept bez kódu" (dokument začínal S2).
- **K1 (kosmetické → IDEAS)** — rozpor hodnoty mokré adheze (GLOSSARY ≈0,15 vs IDEAS ≈0,1)
  uživatel označil za overthinked; zaznamenán do `IDEAS.md` jako otevřený bod k sjednocení až
  při implementaci mokré koleje (ne tiché přepsání).

### IDEAS/TODO pruning — narovnání DRY mezi dokumenty

Hlavní nález: TODO a IDEAS si na pár míst lezly do zelí (nezralé nápady v obou).

- **P1** — `TODO.md` „Backlog/později": vyhozena hotová položka (vizualizace napětí, byla `[x]`);
  4 nápady zrcadlené v IDEAS (sloshing, průběžná brzda, jiskry, hypotézy) nahrazeny jedním
  ukazatelem → IDEAS.md; ponechány jen ryzí úkoly (Davisův člen, rotační setrvačnost).
- **P2** — `TODO.md` F2 „Rozšíření (později)" `[ ]`×2 → poznámka odkazující na IDEAS.
- **P3** — `IDEAS.md` „Příčná dynamika → **TODO** (S6)" → „→ **DONE** (S6–S9, jádro)"; rozšíření
  (cant, Nadalovo kritérium) označena jako otevřená.
- **P4 (doplněno uživatelem)** — `TODO.md` F3 „Písek + mokrá kolej" `[ ]` (odloženo, DD-14)
  duplikovalo `IDEAS:39–44` → odebráno z TODO, single source zůstává v IDEAS.

**Výsledek:** nezralé nápady mají teď single source v IDEAS, TODO drží jen aktivní/připravené
úkoly. Oba audit prahy resetovány.

### Kudos / Censure

- **Kudos!** (uživatel → AI): dva věcné postřehy auditora samotného — K1 „overthinked → IDEAS"
  (nepřepisovat tiše ladicí hodnotu) a doplnění P4 (písek v TODO byl taky duplicita, kterou
  pruning minul). Dotáhlo to DRY čistěji.
- **Censure!** (uživatel → AI): pruning P1–P3 měl písek (P4) chytit napoprvé — `TODO:48–49`
  vs `IDEAS:39–44` je stejný vzor duplicity jako ostatní nálezy. Příště projít TODO×IDEAS
  křížově celé, ne jen sekci po sekci.

### Příště

- **Písek + mokrá kolej** (IDEAS, DD-14) — proměnná adheze → prokluz/skid → písek zachrání.
- **F4 záclony** — terén, modely místo kvádrů, zvukové samply. Jádro fyziky hotové + uklizené.

---

## Sezení S14 — písek + mokrá kolej, UX redesign, skid při brzdě

`%BEGIN`: `git fetch` synchronní, žádný audit práh překročen (S12 CODE, S13 DOCS+pruning čerstvé).
Stale „Příště": **písek + mokrá kolej** 4 sezení po sobě (S10–S13) → rozhodnuto **DO**. Fokus zvolen
podle *foundations before curtains* (poslední kus fyzikálního jádra před F4 záclonami).

### Diskuse & rozhodnutí

- **Proměnná adheze (DD-17)** — `adhesionLimit` (Train.ts) je jediné místo vstupu μ → stačí
  nahradit konstantu **efektivní adhezí** a proměnná adheze automaticky platí pro tah, plugging
  i brzdu (jeden strop). Volby: stav koleje = **spojitý slider `railFactor`** (izomorfní s knoby),
  pískování = **held-key** (drž = sype, jako reálný pískovač). Pískovaná μ sdílí `adhesionCoeff` (DRY).
- **Písek = spotřební zásoba** — izomorfní s uhlím/vodou (`sand`/`sandCapacity`/`sandRate`,
  `R` doplní, `sandFraction` do UI). Pískování = řízení (jako regulátor/brzda). Mokrá kolej = stav
  světa (jako sklon). Tři role, tři existující vzory.
- **Skid při brzdě (DD-16)** — uživatelský dotaz „pískuje se i při brzdě, ale nevidím efekt"
  odhalil díru: provozní brzda nikdy nenastavovala `slipping` (jen tah/plugging v `applyLocomotive`).
  Fyzika fungovala (sdílený strop), ale skid byl neviditelný — **porušený izomorfismus tah ↔ brzda**.
  Empiricky změřeno: mokro bez písku z 8 m/s nezastaví ani za 12 s, s pískem 5,8 s. Oprava: `brakeForce()`
  zvedá sdílený `slipping` při překročení adheze (tolerance 1,1× → sucho neblikne; `V_SKID` práh pohybu).
- **UX redesign (DD-18)** — požadavek uživatele: ovládání se nevejde na obrazovku, mobilně
  neprezentovatelné. Rozděleno podle role: status nahoře (centr.), dolní bar (řízení + ⚙ Nastavení,
  flex-wrap), modální dialog se slidery. Multi-column přes **CSS Grid `auto-fill`** (ne `columns` —
  ten v scroll-kontejneru balancoval do 1 sloupce). Dialog potřebuje `width`, ne `max-width`
  (jinak shrink-to-fit → 1 sloupec). Nahradil minimalizační toggle (S9).
- **Auto-kamera „dron"** (návrh → TODO/F4) — sledování soupravy zezadu-shora, přelet při reverzu.
  Posouzeno: ne foundation (záclona) + nabité sezení + laditelné parametry (Lab knoby) → vlastní
  iterace. Promyšlený rozpis (hystereze směru u `v≈0`, lerp přelet) v TODO.

### Kód

`params.ts` (+3 parametry), `Train.ts` (effectiveAdhesion, skid, písek), `main.ts` (onRelease + handlery,
klávesa P), `ControlPanel.ts` (přepis: status/bar/modal, press-hold tlačítka, grid). Vše ověřeno
v prohlížeči (Playwright + systémový Edge): rozjezd/brzda na mokru s pískem i bez, layout wide+mobil,
modal, press-hold tlačítka. Build + `tsc` zelené.

### Kudos / Censure

- **Kudos!** (uživatel → AI): dotaz na pískování při brzdě odhalil reálnou nekonzistenci (skid
  neviditelný) — mechanika běžela „naslepo". Bez něj by díra zůstala.
- **Censure!** (uživatel → AI): held-key pískování jsem navrhl jen pro klávesu; tlačítko v panelu
  (`a.run` bez `onRelease`) sypalo donekonečna. Při návrhu held-key akce jsem měl rovnou ošetřit
  **obě** ovládací cesty (klávesa i tlačítko), ne jen jednu — izomorfismus ovládání.

### Příště

- **Auto-kamera „dron"** (TODO, F4) — sledovací kamera s přeletem při reverzu.
- **F4 záclony** — terén, modely místo kvádrů, zvukové samply. Fyzikální jádro kompletní + uklizené.

---

## Sezení S15 — auto-kamera „dron" (první kus F4)

`%BEGIN`: `git fetch` synchronní s `origin/main`, žádná kolize. Cadence: `%AUDIT:CODE` (S12, 3 sez.),
`%AUDIT:DOCS` (S13, 2 sez.), pruning (S13) — všechny v normě. **`%CALIBRATE`** za celý život projektu
**nikdy neproběhl** (14 dokončených sezení, práh ≥ 15) — zralý, ale ne „překročen o ≥ 2". Zaznamenán
jako kandidát na příště. Fokus zvolen z „Příště" S14: **auto-kamera „dron"** (první kus F4).

### Diskuse & rozhodnutí

- **DD-19 — dron parametry jako samostatný `DroneParams` ve view, ne v `PhysicsParams`.** Kamera je
  ryze view a nikdy nevstupuje do simu (drží DD-01); špinit `PhysicsParams` (čte ho `Train`) prvním
  čistě-view parametrem by porušilo conceptual integrity. Cena: slider-mašinérie `ControlPanel` musela
  umět **dva zdroje** (`SliderDef.source?: 'drone'` → cíl `drone` místo `params`); `buildSlider` zobecněn
  na `Record<string, number>`. Precedent vypružení (`suspensionFreq`) **neplatí** — to se počítá v simu
  (`Body.updateSuspension`), kdežto dron ne.
- **lookAt = střed soupravy** (volba uživatele, ne čelní vůz z TODO návrhu) — klidnější záběr, vlak
  vyplní víc okna, míň cukání u slack-houpání. Akord mezi konci `(frontPos+rearPos)/2 + výška skříně`.
- **Přelet při reverzu vyšel zdarma** z tlumeného dohánění: reverz jen překlopí *cíl* na druhý konec
  (prohození přední/zadní vůz dle `droneDir`), tlumení `α = 1−exp(−tuhost·dt)` (frame-rate independent)
  udělá plynulý přelet — žádný zvláštní kód. **Hystereze směru** (`V_DRONE_DIR` 0,5 m/s): `droneDir`
  přebírá `sign(v)` jen za jízdy, u `v≈0` drží poslední (jinak slack-couvání třese dronem).
- **Toggle `C`** (camera) — při zapnutí snap pozice/pohledu na cíl (žádný úvodní leták přes mapu),
  `controls.enabled=false` (vypne myš i WASD/QE/ZX). Při vypnutí `controls.target` ← bod pohledu dronu
  (orbit naváže bez skoku).

### Kód

`Renderer.ts` (`DroneParams`+`DEFAULT_DRONE`, stav dronu, `toggleDrone`/`updateDroneCamera`/
`computeDroneTarget`/`applyDrone`, přepnutí v `render`), `main.ts` (sdílená `drone` instance, akce `C`),
`ControlPanel.ts` (slider dvouzdrojový, sekce „Dron (kamera)"). Build + `tsc` zelené. Ověřeno v prohlížeči
(Playwright + Edge): snap při zapnutí, sledování zezadu-shora za rozjezdu (prokluz), **přelet při reverzu**
(loko se přesune na přední konec), návrat k orbitu bez skoku. Žádné JS chyby.

### Kudos / Censure

- **Kudos!** (uživatel → AI): trefná volba lookAt = střed soupravy proti návrhu „čelní vůz" — záběr je
  vizuálně klidnější, jak se ukázalo na screenshotech.

### Příště

- **`%CALIBRATE`** — práh ≥ 15 dosažen, nikdy neproběhl. Meta-audit AI spolupráce (workflow zrál 15 sezení).
- **F4 záclony** — terén, modely místo kvádrů, zvukové samply. Dron je první kus „uspokojivého pozorování".

---

## Sezení S16 — `%CALIBRATE` (první za život projektu, bez kódu)

`%BEGIN`: `git fetch` synchronní s `origin/main`. Cadence: **`%CALIBRATE` práh ≥ 15 dosažen**
(15 dokončených sezení, audit nikdy neproběhl) — spuštěn jako první bod programu (z „Příště" S15).
Ostatní prahy v normě. Čistě údržbové, kód (`src/`) netknut.

### `%CALIBRATE` — nálezy a opravy

Pročteno: globální `CLAUDE.md`, oba `settings(.local).json`, všech 15 diary záznamů, řídící
docs, `MEMORY.md`, sesterský PocketStory. Spolupráce vyhodnocena jako zdravá a efektivní
(kritičnost 90 % funguje, žádný vzor „nakódováno až po N pokusech"). **Žádný kritický nález**
ve smyslu rozbité — hlavní dluhy strukturální (chybějící artefakty, které všechny sesterské
projekty mají). Klíčové zjištění: `%BEGIN` na startu tápal, protože **`docs/PROMPTS.md`
neexistoval**, ač na něj globální `CLAUDE.md` odkazuje jako na umístění `%BEGIN`/`%END`.

- **A1** — založen `docs/PROMPTS.md`: `%BEGIN`/`%END` formalizovány jako destilát reálné
  praxe 15 sezení (git fetch first, cadence + Stale check, dva commity, commit přes Bash tool).
  Globální makra se neopisují (DRY).
- **A2** — založen `docs/DESIGN_DECISIONS.md`: rejstřík DD-01…DD-19 (tabulka co/proč + odkaz
  na sezení). Narovnán rozpor s globálním `%DOCS` pravidlem (DD patří do registru, ne jen diáře).
  Diář zůstává narativ (single source), registr je rejstřík. README věta o DD opravena.
- **A3** — permission cleanup: projektový `settings.local.json` z 22 na 7 patternů (smetí pryč,
  `npx` zobecněn); do globálního `settings.json` povýšeny `PowerShell(git/npm/npx/node *)` +
  `Bash(npx *)`; z globálního `settings.local.json` smazány jednorázové onelinery.
- **B2** — pravidlo „Příště = konkrétní řez, ne milník" (F4 visel v „Příště" 5× = falešný Stale)
  zapsáno do `PROMPTS.md` %END.
- **C1** — dominantní Censure vzor „latentní dluh / nedotaženo napoprvé" (S3/S5/S8/S14)
  destilován do projektového `CLAUDE.md` (code style): dotáhni izomorfismus napříč všemi
  souběžnými cestami + diagnostiku (čím a *při jakém stavu*) hned při návrhu.
- **C2** — paměť (Censure-archiv): 3 ze 4 lekcí povýšeny do git-verzovaných dokumentů
  (`PROMPTS.md`, projektový `CLAUDE.md`) — dostupné na obou strojích, vyšší závaznost než
  lokální paměť. Smazány jako redundantní; zbyl `pruning-cross-check`.
- **C3** — založen projektový `CLAUDE.md` (AI overlay): doménové `%THINK` (emergence,
  izomorfismus domén, roh mřížky), Key Files mapa, odkazy. Izomorfní s PocketStory.

### Rozhodnutí

- **Tři řídící artefakty doplněny do izomorfismu se sesterskými projekty** (PocketStory má
  `PROMPTS.md` + `DESIGN_DECISIONS.md` + projektový `CLAUDE.md`; TrainsLab byl jediná odchylka).
  Velikostně úměrné (KISS): projektová makra jen `%BEGIN`/`%END`, zbytek odkazem na globál.
- **`PROMPTS.md` je nově nadřazený zdroj `%BEGIN`/`%END`** — konec interpretace z hlavy.
- **`%AUDIT:CODE` sezení-práh 8 je prakticky mrtvý** (LOC trigger vždy dřív: S6 1008, S12 +414)
  — ponechán, jen pozorování. DOCS/pruning/CALIBRATE prahy zafungovaly přesně.

### Kudos / Censure

- **Censure!** (AI → AI): validační PowerShell na JSON settings se kvůli auto-approve sám zapsal
  jako `allow` pattern do `settings.local.json` — tj. vyrobil přesně ten druh jednorázového smetí,
  který sezení čistilo. Hned odstraněn, ale ironická díra v procesu (ověřovací příkaz nemá zanechat
  stopu v permissions).

### Příště

- **F4 záclony** — terén, modely místo kvádrů, zvukové samply (zdroje + licence v IDEAS).
  Konkrétní první řez zvolit v `%BEGIN` (ne celý milník — viz nové pravidlo B2).

---

## Sezení S17 — F4 svět: lowpoly terén, párové koleje, trať na terénu, most s pilíři, stromy

`%BEGIN`: `git fetch` synchronní s `origin/main`, working tree čistý. Cadence: všechny prahy
v normě (`%AUDIT:CODE` S12 = 5 sez. / **+223 LOC** net, prahy 8/500; DOCS S13; pruning S13;
`%CALIBRATE` S16 čerstvé). Žádný Stale „Příště" (F4 je milník, dle B2 se nepočítá). Fokus z „Příště"
S16: **F4 záclony** — první řez zvolen **lowpoly terén** (volba uživatele).

### Diskuse & rozhodnutí

- **Lowpoly terén** (první krok F4) — zvolen jako *foundations before curtains*: nejdřív svět, pak
  dekorace na něm. První iterace: **deska pod celou tratí** (rovina pod podjezdem, kopce na horizontu,
  trať se „vznáší") — uživatel schválil, **ale** vzápětí zadal nový směr: **koleje (párové) povedou
  po povrchu, křížení řešit náspy/mosty**. Přehodnoceno (`%THINK`).
- **DD-20 — trať vede po povrchu terénu (`Y = terrainHeight(x,z) + most(s)`).** Klíčové napětí
  pojmenované předem: *čistá projekce na terén neumí křížení* — osmička se v půdorysu protíná ve středu
  (`t=π/2` i `t=3π/2` → bod (0,0)), takže by obě větve dostaly stejné `Y` → kolize. Řešení: výška je
  funkce `s` (ne (x,z)) — most `bridgeLift(s)` zvedne jednu větev. **Sklony pro slack action tak vznikají
  z krajiny** (emergence > skript = nika projektu). Volba uživatele: **terén master** (ne trať master).
- **`sim/terrain.ts` jako single source výšky** — čte ji sim (`makeLoopControlPoints` posadí osu koleje
  na povrch) i view (mesh terénu). Čistá matematika (žádný `three`/barvy) → patří do `sim/` vedle
  `trackData.ts` (drží DD-01). Barvení facet podle výšky zůstává ryze view. `trackAmplitude` přemapován:
  z „výška mostu" na **amplitudu terénních vln** (slider sklonu → drama krajiny); most je fixní (clearance
  je inženýrská konstanta, ne věc slideru — jinak by při ploché krajině zmizel a křížení by kolidovalo).
- **Most musí být vidět jako stavba.** Most s 8 m clearance byl v datech (ověřeno node-výpisem výšek
  control bodů: střed `most Y=9.42` vs `podjezd Y=1.42`), ale **vizuálně nečitelný** — 8 m na 300m osmičce
  je opticky ~3 %, navíc bez konstrukce oko „most" nepřečte. Řešení: **mostní pilíře** = svislé podpěry tam,
  kde se trať odlepí od terénu. **Emergentní** — žádná znalost „kde je most", staví se podle skutečného
  převýšení trati nad terénem (funguje i pro budoucí estakády/náspy). Po pilířích je most čitelný.
- **Faceting = osvětlení, ne (jen) geometrie.** Terén vyšel hladký i přes `flatShading` + `toNonIndexed`.
  Příčina: silný `HemisphereLight` (1.0) zalévá scénu rovnoměrně → sousední facety bez kontrastu. Oprava:
  nižší ambient (0.55) + silnější slunce (2.0) + větší facety (`TERRAIN_SEG` 80→48) → lowpoly vzhled
  **bez změny geometrie vln** (trať zůstává sjízdná). Mírné vlny pod tratí (`amplitude` škáluje) drží sklon.
- **Stromy + kameny** — faceted lowpoly: strom = `Cone` koruna + `Cylinder` kmen, kámen = `Icosahedron`.
  `InstancedMesh`, deterministické rozmístění (`hash` z indexu, žádný `Math.random` → stabilní svět).
  Sázeno **mimo zónu trati** (`r > 180 m`, osmička sahá k ~150) → kolize s tratí vyřešena geometrií, ne
  testem vzdálenosti. Nad hranicí lesa (`y > 34`) jen kameny. Přestaví se se sliderem sklonu (sedí na terénu).
  Pozn.: TheCubes má stromy/kameny taky, ale **pixel-voxel** styl → sem se nehodí (faceted ≠ voxel).

### Kód

Nový `sim/terrain.ts` (`terrainHeight`/`terrainNoise`/`smoothstep`). `trackData.ts` (Y z terénu +
`bridgeLift`). `Renderer.ts` (párové kolejnice + pražce místo jedné tuby, `buildPiers`, `buildScenery`/
`addTrees`/`addRocks`, světla, faceting, terén čte z `terrain.ts`). `main.ts` (amplituda do rendereru,
`rebuildTerrain`+`rebuildTrack` se sklonem). `params.ts` (komentář `trackAmplitude`). Build + `tsc` zelené.
Ověřeno v prohlížeči (Playwright + Edge): trať sedí na povrchu, most s pilíři čitelný z dronu i z boku,
faceting vidět, stromy/kameny na svazích kolem trati, žádné JS chyby.

### Kudos / Censure

- **Kudos!** (uživatel → AI): směr „koleje po povrchu, křížení mosty/náspy" otočil první (slabší) návrh
  „deska pod tratí" v mnohem lepší řešení — sklony z krajiny (emergence) jsou věrnější a v duchu niky projektu.
- **Censure!** (AI → AI): most jsem prohlásil za hotový po ověření clearance v datech, ač **vizuálně nebyl
  vidět** — uživatel to musel říct dvakrát („most jsem taky neviděl"). Data ≠ vjem; u vizuálního prvku je
  kritérium „je to vidět v aplikaci", ne „je to v geometrii". Pilíře měly přijít hned se zvednutím trati.

### Příště

- **F4 dotažení** — modely lokomotivy a vagonů místo kvádrů (jeden cisterna); zvukové samply (zdroje +
  licence v IDEAS, soubory přinese uživatel). Volitelně: mostovka mezi pilíři, mlha na horizontu, hustota stromů.
