# Diář — 2026-06-02

Kontext a rozhodnutí. Sezení číslovaná (S1…), DD = Design Decision.

## Sezení S31 — ztlumení clunk + %AUDIT:CODE (split Renderer → WorldView)

`%BEGIN` na stroji mrkla, git čistý a sync. Cadence: `%AUDIT:CODE` na sezení-prahu (S25 = 6 sez.;
LOC +193 v `src/`, pod prahem 250 — net dokonce −100 po smazání `proceduralAudio` v S27), ostatní
prahy v normě. Backlog konkrétních fyzikálních položek vyčerpán (S30). Uživatel: drobná úprava zvuku,
pak audit.

### Ztlumení clunk.wav

Clunk se přehrával ve dvou kontextech přes `playClunk` (výhybka/křížení `playClunk(0.7)`, nárazník
`playClunk(volume/3)`). Volba uživatele: **ztlumit celý sample** (oba kontexty) → faktor 1/3 do
`playClunk` (single source pro clunk sample). Vedlejší efekt (přijatý): nárazník spadl na ~1/9
(relativní /3 × globální /3) — výhybka zůstává hlasitější.

### `%AUDIT:CODE` (6. audit; 0 kritických) — „opravit vše"

Přečten celý `src/` (16 souborů, ~2300 ř.), GLOSSARY, DESIGN_DECISIONS, struktura repa, public.
Fyzikální vrstvy přidané od S25 (μ(v) brzdy, `davisB`, `curveResistance`, rotating mass) ověřeny
izomorfní a správně oddělené (síly drží skutečné `m`, jen `integrate` dělí `m_eff`; geometrické vs.
rychlostní členy v `beginStep`). Žádné debug výpisy / mrtvý kód / mrtvé soubory, `dist/` gitignored,
všech 10 samplů v `public/audio/` se používá. Nálezy → **vše opraveno** (na přání uživatele):

- **K1** — `playClunk` magic `1.1/3` → konstanta `CLUNK_GAIN = 1.1/3` s komentářem.
- **K2** — nekonzistentní pořadí cross-productu pro příčnou osu kolejnic (`crossVectors(tan, UP)` vs.
  pražce/pilíře `crossVectors(UP, tan)`) → sjednoceno na `crossVectors(UP, tan)`. Čistě kosmetické
  (offset `±gauge/2` je symetrický → geometrie identická), `right` přejmenován na `side`.
- **D2** — doc drift zavedený dnešní změnou clunk: `GLOSSARY:178` tvrdil „clunk na 1/3 (S27)", už
  neplatí → přepsáno (globální `CLUNK_GAIN` + relativní /3 nárazníku).
- **D1 (split Renderer, otevřený dluh KO3 z S18)** — `Renderer.ts` (436 ř.) míchal stavbu statického
  světa s render loopem aktérů. Vytažen **`view/WorldView.ts`** (terén + dekorace + trať/pilíře, metoda
  `rebuild(amplitude)` na slider sklonu, export `RAIL_RADIUS`). Renderer zhubl na ~210 ř. a řeší jen
  aktéry (vozy, markery spřáhel, kouř) + kameru + loop. `main.ts`: dva `rebuildTerrain`+`rebuildTrack`
  → jeden `rebuildWorld`. Symetrické se S25 (CameraController/AudioView), SLAP, drží DD-01.

### Kód

`AudioView.ts` (`CLUNK_GAIN`), `WorldView.ts` (nový, ~310 ř.), `Renderer.ts` (zeštíhlen, import
`WorldView`+`RAIL_RADIUS`), `main.ts` (`rebuildWorld`), `trackData.ts` (komentář `rebuildTerrain` →
`rebuildWorld`). Docs: GLOSSARY (clunk + termín WorldView), README (strom view), CLAUDE.md (Key Files),
`docs/PROMPTS.md` (cadence ledger CODE → S31). `tsc --noEmit` + `npm run build` zelené (**21 modulů**,
+1 = WorldView). Uživatel ověřil v prohlížeči: **„Test OK"**. **Žádné nové DD** (split = aplikace SLAP
jako S25, ne architekturní rozhodnutí).

### Příště

- **F4 záclony — volitelné dotažení:** mostovka mostu, mlha na horizontu (z TODO `[~]`).
- Nebo sáhnout do `IDEAS.md` (sloshing F5, průběžná brzda soupravy, dynamický prokluz, jiskry) — tyto
  vyžadují `%THINK` (roh mřížky / nový DOF, pozor na DD-02/DD-04).
- Cadence: všechny prahy čerstvě v normě (CODE právě resetován S31).

## Sezení S32 — F4 dotažení (mlha + mostovka), dron orbit, toggle markerů, fix prokluzu kol

`%BEGIN` na stroji mrkla, git čistý a sync. Cadence: všechny prahy v normě (CODE resetován S31).
Z „Příště" zvoleno **F4 dotažení**; v průběhu přibyly další požadavky uživatele (čistá scéna,
chování dronu) a jeden vizuální bug fix. Vše view-only (drží DD-01), žádný zásah do simu.

### Mlha na horizontu + mostovka (F4 záclony)

- **Mlha** (`Renderer.ts`) — `scene.fog = THREE.Fog(0xccd6dd, 130, 340)`, **bělavý opar** (volba
  uživatele proti barvě nebe). Fog počítá vzdálenost od kamery → souprava i blízké stromy ostré,
  jen okraj terénní desky (±350 m) vybledne. Patří k Rendereru (atmosféra scény), vedle `background`
  a světel — geometrie zůstává ve `WorldView` (dělba z S31).
- **Mostovka** (`WorldView.ts`) — souvislý nízký betonový nosník pod kolejnicemi v úsecích, kde je
  trať vyvýšená nad terén. **Plná deska** (volba uživatele, ne příhradovina — KISS, lowpoly styl).
  Box-segmenty `InstancedMesh` hustě podél trati (`DECK_SPACING=2 m`, segment ×1.4 → překryv = souvislost
  i v oblouku), orientace sleduje tečnu i sklon koleje, `DECK_DROP=0.35 m` pod pražce.
- **DRY refaktor** — detekce vyvýšení trati (`clearance > práh`) byla jen v `buildPiers`; vytažena do
  sdíleného **`elevatedSamples(amplitude, spacing)`** (vrací body+tangenty+výšky). Pilíře jedou přes
  něj s řidším vzorkováním (`PIER_SPACING=6`), mostovka s hustým. Emergence drží: žádná znalost „kde
  je most", funguje i pro budoucí estakády/náspy.

### Markery napětí spřáhel → toggle (čistá scéna)

Uživatel chtěl odstranit „nárazníkové koule". Oponováno: jsou to **markery napětí ve spřáhlech**
(osciloskop slack action, F1 ★) — barva tah/tlak, jas ∝ síla. Kompromis (volba uživatele): **toggle
checkbox v Nastavení**, default **skryté** (čistá scéna), Lab nástroj na vyžádání.

- `Renderer.ts` — flag `couplerMarkersVisible` (default false), `setCouplerMarkersVisible(v)` (viditelnost
  + skip `renderCouplers` když skryté = zbytečná práce), `buildCouplers` staví skryté.
- `ControlPanel.ts` — nová sekce **„Zobrazení"** v gridu Nastavení + helper `buildCheckbox` (boolean
  přepínač mimo params, izomorfní s `buildSlider`); `PanelHandlers.onCouplerMarkers`.
- `main.ts` — handler `renderer.setCouplerMarkersVisible`.

### Dron: chase → orbit kolem lokomotivy (revize DD-19)

Požadavek uživatele: „dron ať kolem jedoucího vlaku krouží", pak „zaměřuje lokomotivu, ne střed
soupravy" + zoom i v dronu (Z/X i kolečko myši). Orbit **nahradil** chase (volba uživatele).

- `CameraController.ts` — `updateDroneCamera`/`computeDroneTarget` přepsány: kamera obíhá střed orbity
  = **lokomotiva** (`bodies[0]`) konstantní úhlovou rychlostí (`orbitAngle += orbitSpeed·dt`), drží
  poloměr (`distance`) + výšku, `lookAt` lokomotiva. Pozice/pohled se k cíli tlumeně dohánějí
  (`stiffness`, `α=1−exp`). **Odpadla** hystereze směru (`droneDir`, `V_DRONE_DIR`) i reverz-přelet
  (orbit je na směru jízdy nezávislý → kód se zjednodušil).
- **Zoom v dronu** — `Z/X` (drženo) i **kolečko myši** mění poloměr kroužení jednou cestou
  `adjustOrbitRadius(delta)` (DRY; v ručním režimu kolečko dál řeší `OrbitControls`). `wheel` listener
  na canvasu se v ručním režimu hned vrací.
- **K DRY (dotaz uživatele „proč dvě ovládání kamery"):** jsou to dva *režimy* jedné třídy (ruční vs.
  dron = state machine), ne duplikace. Skutečná drobná duplikace byl jen zoom — konsolidován v rámci
  dronu. Ruční zoom záměrně ponechán na `OrbitControls` (pinch/limity zdarma; přepis = krok zpět).
- `DroneParams` + `orbitSpeed` (default 0,3 rad/s ≈ 21 s/okruh); slidery: „Poloměr kroužení" (přejm.
  odstup), „Rychlost kroužení", výška + tuhost beze změny.

### Bug fix — prokluz kol se točil obráceně

Uživatel: „u prokluzu se kola točí obráceně, možná strobo". Ověřeno = **znaménkový bug**, ne aliasing.
Valecí fáze jde jako `-body.s/r` (vpřed = klesá), ale slip přírůstek byl při `notch≥0` **kladný** →
kola se při prokluzu protáčela dozadu. Prokluz = obvodová rychlost kol > rychlost vlaku (kola hrabou
**vpřed** rychleji), ne couvání. Oprava: `dir = notch≥0 ? -1 : 1` (slip ve směru valení). Promítne se
i do hnacích spojnic (sledují tutéž `phase`).

### Kód

`Renderer.ts` (fog, markery toggle, slip dir), `WorldView.ts` (mostovka + `elevatedSamples`),
`CameraController.ts` (orbit), `ControlPanel.ts` (sekce Zobrazení + `buildCheckbox` + dron slidery),
`main.ts` (handler markerů). `tsc --noEmit` + `npm run build` zelené. Změna chování dronu (chase →
orbit) — **DD-19 drží beze změny** (rozhodnutí je „`DroneParams` ve view", ne konkrétní chování kamery),
aktualizován jen GLOSSARY termín „dron". Žádné nové DD. Vizuál ověřuje uživatel (dev server).

### Příště

- **F4 záclony — zbylé volitelné:** (mlha + mostovka hotové). Případně mlha/mostovka doladění čísel.
- Nebo `IDEAS.md` (sloshing F5, průběžná brzda, dynamický prokluz, jiskry) — vyžadují `%THINK`.
- Cadence: CODE naposledy S31 (1 sez.), ostatní v normě.
